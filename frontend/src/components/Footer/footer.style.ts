import { SxProps } from '@mui/material/styles';

const footer: SxProps = {
  textAlign: 'center',
  padding: 2,
  backgroundColor: 'primary.main',
  marginTop: 5,
};

const footerText = {
  color: 'black'
};

export const styles = {
  footer,
  footerText,
};