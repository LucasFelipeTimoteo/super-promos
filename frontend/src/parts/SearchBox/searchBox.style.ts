import { SxProps } from "@mui/material";

const searchboxWrapper: SxProps = {
  display: 'flex',
  justifyContent: 'center',
  marginBottom: '16px'
}

const textField: SxProps = { marginRight: '8px', width: '300px' }

export const styles = {
  searchboxWrapper,
  textField
}