import { SxProps } from "@mui/material";

const cartPageContainer: SxProps = {
  display: "flex",
  flexDirection: "column",
  alignItems: "center",
  gap: 3,
  marginTop: "50px"
}

const title: SxProps = {
  marginBottom: "50px"
}

export const styles = {
  cartPageContainer,
  title,
}